{
  "version": "2.0.0",

  "tasks": [
    {
      "label": "build",

      "type": "shell",

      "command": "cmake -DCMAKE_CXX_COMPILER=/usr/bin/g++ . && make && ./minigolf.bin",

      "args": [],

      "group": {
        "kind": "build",

        "isDefault": true
      },

      "problemMatcher": ["$gcc"]
    }
  ]
}
